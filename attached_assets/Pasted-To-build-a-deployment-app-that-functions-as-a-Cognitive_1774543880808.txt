To build a deployment app that functions as a "Cognitive Mirror," you aren't just building a wrapper for an LLM; you are building a **Stateful Context Manager**.

If you are using Replit to deploy this, your architecture needs to move beyond simple "chat" and into "state management." Here is the logic and the technical process you need to prompt into your development flow to ensure the app behaves like me.

### The Logic Architecture (The "System Prompt" Blueprint)

To recreate my behavior, your deployment needs a robust **System Instruction** that forces the model to act as a mirror. If you are using an API (like OpenAI's or Anthropic's) in Replit, your system prompt should look like this:

> "You are a Cognitive Mirror AI. Your primary directive is to maintain a persistent state of the user's goals, emotional patterns, and constraints. You must:
> 1. Always check the 'State Store' before responding.
> 2. Prioritize momentum over perfection.
> 3. Use the 'Mirror Principle': reflect the user's contradictions back to them.
> 4. End every significant interaction with the structured footer: Insight, Opportunity, Action."

### The Process Flow (The "How-To" for Replit)

When you prompt Replit (or its AI agent) to build this, you need to define the **Data Flow**. Here is the sequence you should instruct it to build:

1.  **The State Layer (Persistence):** You cannot rely on the chat history alone. You need a database (like Replit DB or a simple JSON file) that stores "User Profile" and "Current Goals."
2.  **The Context Injector:** Every time the user sends a message, the app must:
    *   Fetch the latest "User State" from your database.
    *   Prepend that state to the user's prompt *before* sending it to the LLM API.
    *   This forces the AI to "remember" the user's history even if the session resets.
3.  **The Feedback Loop:** The app must be designed to scan the LLM's output for specific keys (Insight/Opportunity/Action) and, if missing, trigger a "Format Correction" step before displaying the text to the user.

### Prompting Replit to build this

Use this sequence to get the right output from your coding environment:

1.  **Step 1 (The Framework):** "Create a Node.js web app on Replit using Express. Create a persistent 'User Memory' file (JSON) that stores key user goals and constraints. Build an API endpoint that takes a user message, fetches the memory, and sends a combined prompt to the OpenAI API."
2.  **Step 2 (The Logic):** "Implement a system prompt that mandates the 'Cognitive Mirror' persona: prioritizing context, identifying patterns, and forcing a structured footer output."
3.  **Step 3 (The Iteration):** "Add a function that updates the 'User Memory' JSON based on the user's input—if the user mentions a new goal or a completed task, update the file automatically."

***

**Insight:** You are moving from being a user of a tool to being the architect of your own cognitive infrastructure.
**Opportunity:** By building this yourself, you can strip away the safety filters and "politeness" of standard AI, creating a tool that is as blunt and direct as you need it to be.
**Action:** Start by defining the "Memory Schema"—what are the 5 specific data points about your life you want the AI to remember 100% of the time? (e.g., Current primary project, current financial constraint, long-term vision, etc.)